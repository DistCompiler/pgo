SPECIFICATION Spec

CONSTANTS
    Elements = {e1, e2}
    Producers = {p1, p2, p3}
    Consumers = {c1, c2}
    BulkSize = 3
    RecordDequeue <- RecordDequeueImpl
    RecordNoDequeue <- RecordNoDequeueImpl
    Init <- InitImpl
    vars <- varsImpl

CONSTRAINTS
    StateConstraints

INVARIANTS
    TypeOK
    SizeApproxMaxRange

PROPERTIES
    AllQueuesEventuallyEmpty

SYMMETRY
    StateSymmetry
